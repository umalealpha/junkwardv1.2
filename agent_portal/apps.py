from django.apps import AppConfig


class AgentPortalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'agent_portal'
    verbose_name = 'Agent Portal (Sales Commissions)'
