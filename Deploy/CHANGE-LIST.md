# CHANGE-LIST — what is being worked on RIGHT NOW

**Fill this in BEFORE you start editing Omni.** One line per change. This is how two chats
avoid quietly editing the same thing and colliding when they go live.

**Rules:**
- Add your row when you START. Set Status to `building`.
- Before you edit, **scan the table**. If another row (Status `building` or `waiting`) touches the
  **same area**, STOP and coordinate — do not both edit it.
- Update your Status as you go: `building` → `waiting` (finished, checked, ready) → `live` (deployed).
- Once it's `live` and confirmed working, delete your row (or move it to the "Recently shipped" note at the bottom).
- Lane: 🟢 normal or 🔴 emergency.

| Date started | Change (one line) | Area of Omni it touches | Lane | Who / which chat | Status |
|---|---|---|---|---|---|
| _example_ | Add PDF export to payslips | payroll / payslips | 🟢 | Prathap – chat A | building |
| 2026-09-16 | AML registers made recordable: sanctions/PEP screening, supplier screening, AML training and the quarterly Board report get endpoints + screens; risk-register writes no longer gated on the `cfo`/`iso_auditor` groups, which do not exist on prod (only a superuser could write) | iso_compliance (aml_views/aml_serializers), alpha_finance/api_router.py, core/compliance_dashboard.py, frontend /compliance/aml/* + Sidebar | 🟢 | Prathap – Windows (aml-register-screens) | building |
| 2026-08-24 | Archived leavers hidden from Payroll Register (terminated-but-not-archived stay for final settlement); archived AND terminated hidden from People Directory + HRIS "Humans" count; HR reveal via ?archived=1 | payroll/api_views.py (PayslipViewSet), hris/api_views.py (employees) | 🟢 | Prathap – Windows (archived-employee-propagation) | live 24-Aug |

| 2026-09-07 | Sign-in landing redesigned: enterprise Omni module map + AI capabilities + trust strip on the navy panel (presentational shell only, no auth change) | login landing (AccessPortalShell) / , /login, /staff-login | 🟢 | Prathap – Windows (design/omni-landing-ai) | waiting |
| 2026-09-09 | Time Doctor hours are a FLOOR: brief + reconcile + enforce + /my-omni tile all read live-first and never revise a person's hours DOWN from a snapshot (bugs c82def7f, 5dffc022 — same person twice); stops a full unpaid day being docked off a frozen 0.00h | integrations/td_live.py (new), send_daily_brief, reconcile_workday_records, enforce_td_deductions, core/my_omni_views.py | 🟢 | Prathap – Windows (fix/bugs-2026-09-09) | live 9-Sep (55309d57) |
| 2026-09-09 | Monthly-feedback OmniTasks now CLOSE when the check-ins are filed (bug 13869f41) — 14 managers incl. CFO/CEO were nagged daily for finished work; stale assign_day toasts self-heal too | hris/feedback_task_close.py (new), taskboard/services.py, hris/manager_feedback_actions.py | 🟢 | Prathap – Windows (fix/bugs-2026-09-09) | live 9-Sep (55309d57) |
| 2026-09-09 | Every HTML page marked no-store by ONE rule instead of a 12-route allow-list — /app (phone app), /my-omni, /approvals, /payroll/payslips, /banking/realpay, /m were serving s-maxage=31536000, so installed apps showed a year-old build | frontend/next.config.ts (headers) — affects ALL routes; static assets deliberately unchanged | 🟢 | Prathap – Windows (fix/app-shell-no-store) | live 9-Sep (55309d57) |
| 2026-09-09 | Monthly Manager Return task now CLOSES when the return is filed (and re-opens if the reviewer sends it back) — it never closed before, so the daily nag would have run for ever ✅ merged alongside the monthly-feedback task-close fix — BOTH closers run in `sweep_due_and_overdue` (`feedback_closed` + `cycle_closed`); if you ever touch that block again, keep both | hris/manager_return_task_close.py (new), hris/manager_return_service.py (submit + send_back), taskboard/services.py (sweep_due_and_overdue) | 🟢 | Prathap – Windows (fix/manager-return-task-close) | live 9-Sep (935b6035) |
| 2026-09-09 | **Manager Objectives** — Omni raises ONE task per manager Sunday 00:00 Gaborone, due Wednesday 4pm, and SETTLES IT ITSELF against live Graphite/Omni data (closes when the numbers moved, stays red when they did not; an unreadable counter is never a miss). Each line shows last period + distance travelled so a 2,627-item backlog visibly reduces. Seeded: Kakale Botana 13 objectives (AML/CFT), Oratile Tlhomelang 8 (data protection). 6 new AML/market-conduct registers. First fire **Sun 13-Sep** | hris/weekly_objective_models.py, hris/objective_counters.py, hris/management/commands/weekly_objectives_cycle.py + seed_manager_objectives.py, iso_compliance/aml_models.py, infra/cron/manager-objectives.cron, admin (hris + iso_compliance) | 🟢 | Prathap – Windows (feat/manager-objectives-2026-09-09) | live 9-Sep (1ea70637) |
| 2026-09-09 | The five remaining screens that read the FROZEN Time Doctor snapshot now come through ONE door (hris/hours_for_day): weekly review + Saturday explain floored by the permanent record and FAIL CLOSED if it cannot be read; excuses feed reads live (productive hours); cost-per-hour keeps the stored copy (uncached web request) and a new 15:50 SAST catch-up pull refreshes it for every snapshot-only reader | hris/hours_for_day.py (new), send_weekly_hours_review, send_saturday_explain, send_hours_reminder, leave_excuse_service, reporting/cost_per_hour, infra/cron/timedoctor-pull.cron | 🟢 | Prathap – Windows (fix/snapshot-display-readers) | live 9-Sep (b58a91ba) |
| 2026-09-09 | Four clean-ups on the Time Doctor hours readers: only the slow live pull is cached (the whole-day cache froze HR's own Accept/Reject for 10 min); the manager/HR TD report and the Telegram lookup now come through hours_for_day.floored_rows so neither can show fewer hours than Omni holds; the /my-omni tile drops its inline copy of the same logic; the Telegram lookup resolves the PERSON against the staff roster and the FIGURE through the confirmed account link (L19), refuses when two people match, and fails closed rather than quote a figure it cannot floor | hris/hours_for_day.py, hris/leave_excuse_service.py, hris/leave_excuse_views.py, integrations/timedoctor_views.py, core/telegram_bot/services.py, core/my_omni_views.py | 🟢 | Prathap – Windows (fix/hours-readers-cleanup) | live 9-Sep (e4dad8c2) |
| 2026-09-10 | **CI midnight flake killed** — `rewards/test_workout_sync.py` pinned the wall clock, so the per-local-day paid-workout cap test paid the cap TWICE (today + yesterday) on any run started in the first hour of a Gaborone day: 8 != 4. Blocked PR #808 (Markdown only). Clock frozen in `setUp` (bonu/test_botswana_time.py pattern); assertion NOT loosened. Swept rewards/ — 2 more tests in the same file had it, one already failing (0 != 1); test_step_sync + nexus_engage caps are clean. New in-window regression class at 01:30 Gaborone. TEST FILE ONLY, no engine change | rewards/test_workout_sync.py | 🟢 | Prathap – Windows (fix/rewards-midnight-cap-freeze, PR #817) | **merged 10-Sep (ba5a68d6) — NO DEPLOY NEEDED, do not bundle: a test file does not run in prod, the benefit landed on merge (CFO decided 10-Sep)** |
| 2026-09-11 | **Statutory tax compliance workflow (VAT/PAYE/OWHT/SAT)** — the LIVE Tax Calendar page was computing its own dates in the browser on the PRE-2026 rules and every one was wrong (VAT last-day vs the 25th, PAYE 15th vs 14th, two company-tax payments vs four, annual return 31 Oct vs 28 Jul). Rebuilt server-side with tests pinning each date; `deadlines.ts` deleted. Six-state workflow — marking your own filing done does NOT stop the reminder, only CFO verification closes it; a missed statutory date is a BREACH the CFO alone can close, in writing. Daily sweep 06:30 CAT. Oprah/Kago/Legakwa can move a date; nobody may extend their OWN filing (Fable: the breach could otherwise never be RAISED, only undone). VAT = 25th, prepare-by = 15th, per the CFO. | regulatory/ (tax_calendar.py, tax_workflow.py, tax_api.py, models, migrations 0003-0006), frontend /tax-calendar, infra/cron/tax-compliance.cron | 🟢 | Prathap – Windows (PRs #876, #881, #885) | **live 11-Sep 12:16 (c02a989c)** |
| 2026-09-10 | Telegram Time Doctor lookup no longer prints the raw `last_seen` object — prod was sending the API object `{ip, online, updatedAt}` and the WHOLE dict went into the chat message, leaking an internal IP address. Now renders the moment only, in Botswana time (`2026-09-08 11:39 SAST`); handles the dict shape and a plain string; a naive timestamp is NOT shifted; an unrecognised shape shows nothing rather than being str()'d | core/telegram_bot/services.py (`_fmt_last_seen` new, `time_doctor_query`) | 🟢 | Prathap – Windows (fix/td-last-seen-ip) | waiting |
| 2026-09-15 | **GL classification — TB import** — `infer_type` is first-match-wins and the greedy `110` (Employee costs) entry sat ahead of the 1104xx fixed-asset block, so any NEWLY created Furniture & Fittings / Laptops / Motor Vehicles account, and its `.01` accumulated-depreciation contra, would have been imported as an expense. Nothing live is wrong: existing rows are never retyped on re-import and prod's 110402.01 is already correct. **CFO SIGN-OFF OBTAINED IN WRITING 2026-09-15** (C6 — GL mapping may not change without it); he was shown that no existing data is touched and only future imports were exposed. Rebuilt from the stale 31-Aug branch, and one bug in that branch fixed on the way: it mapped the bare `110402` (Motor Vehicles, COST) to accumulated_depreciation — only `110402.01` is the contra | ledger/management/commands/import_tb_csv.py (PREFIX_TYPE_MAP), ledger/tests/test_infer_type_prefixes.py | 🟢 | Prathap – Mac (fix/tb-map-1104-2026-09-15) | waiting |
<!-- Add new rows ABOVE this line. Keep the example row as a guide. -->

---

### Recently shipped (keep the last ~10, oldest dropped)
- _nothing yet_
