from django.apps import AppConfig


class BoardroomConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'boardroom'
    verbose_name = 'Alpha Rooms — Boardroom Booking'
